VITE_SUPABASE_URL=http://localhost:54321
# The anon key is the same for every local project
VITE_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
VITE_META_APP_ID=
VITE_FB_LOGIN_CONFIG_ID=
# F10: how the chat receives live changes. `postgres_changes` (default) is the
# organization-wide subscription; `broadcast` joins the private channels the
# API publishes to (org:, agent:, conv:) — enable it once the API migration
# `f10_realtime_broadcast` is deployed. Remove the flag when verified.
VITE_REALTIME_MODE=broadcast
